import {
  select,
  scaleOrdinal,
  schemeCategory10,
  forceSimulation,
  forceManyBody,
  forceLink,
  forceX,
  forceY,
  timeout,
  interval,
  now,
} from "d3"

export default {
  select,
  scaleOrdinal,
  schemeCategory10,
  forceSimulation,
  forceManyBody,
  forceLink,
  forceX,
  forceY,
  timeout,
  interval,
  now,
}
